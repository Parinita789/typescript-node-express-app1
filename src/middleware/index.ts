import { Router } from 'express';
import { BasicMiddleware } from './basic.middleware';
// export default ({ config, db }) => {
// 	let routes = Router();

// 	// add middleware here

// 	return routes;
// }


export {
	BasicMiddleware
};
